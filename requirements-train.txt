# Training dependencies for openwakeword custom model training.
# Based on the Colab notebook (automatic_model_training_simple).
#
# Usage:
#   pip install -e .
#   pip install -r requirements-train.txt

--extra-index-url https://download.pytorch.org/whl/cu121

# PyTorch (CUDA 12.1 build, compatible with CUDA 12.2+)
torch==2.5.0
torchaudio==2.5.0

# TTS & audio
piper-tts
piper-phonemize-cross
webrtcvad
soundfile

# Training
torchinfo==1.8.0
torchmetrics==1.2.0
speechbrain==0.5.14
audiomentations==0.33.0
torch-audiomentations==0.11.0
acoustics==0.2.6
deep-phonemizer==0.0.19

# Data
mutagen==1.47.0
pronouncing==0.2.0
datasets>=2.18,<3

# Model conversion
onnx==1.19.1
onnx2tf
onnxsim
onnx_graphsurgeon
sng4onnx
onnxruntime==1.22.1
ai_edge_litert==1.4.0

# Misc
setuptools
numpy<2
pyyaml>=6.0,<7
